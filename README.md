# PostSync

Сервис односторонней пересылки постов из Telegram-каналов в MAX с поддержкой ретроспективной синхронизации истории.

> **Текущий режим работы:** TG → MAX  
> Обратное зеркалирование (MAX → TG) временно отключено и сохранено в закомментированном виде для будущего использования.

## Возможности

- **Кросспостинг TG → MAX** — автоматическая пересылка новых постов из Telegram-канала в MAX-канал
- **Ретроспективная синхронизация** — скачивание истории TG-канала за произвольный период и публикация в MAX через клиентский протокол MTProto
- **Управление через Mini App** — веб-интерфейс прямо в Telegram и MAX: связки, задачи, прогресс синхронизации, профиль
- **Автозамены** — замена текста, URL и паттернов по регулярным выражениям при пересылке
- **Retry-очередь** — при недоступности API сообщения сохраняются в БД и доставляются после восстановления связи
- **Сохранение форматирования** — жирный, курсив, код, ссылки, зачёркнутый, подчёркнутый
- **Живое прослушивание** — переключение активности потока для каждой связки через Mini App
- **Очистка истории** — сброс внутреннего маппинга сообщений за период для повторной отправки
- SQLite или PostgreSQL для хранения данных

### Форматирование при пересылке TG → MAX

| Формат | Поддержка |
|--------|:---------:|
| **Жирный** | ✅ |
| *Курсив* | ✅ |
| `Моноширинный` | ✅ |
| ~~Зачёркнутый~~ | ✅ |
| Подчёркнутый | ✅ |
| [Ссылки](url) | ✅ |
| Цитата | ❌ |
| Спойлер | ❌ |

Цитаты и спойлеры не поддерживаются MAX Bot API.

---

## Установка

### Из бинаря

```bash
chmod +x postsync
./postsync
```

### Docker

```bash
docker run --env-file .env ghcr.io/andkh1976/postsync:latest
```

### Docker Compose (с PostgreSQL)

```bash
cp .env.example .env
# Заполните переменные в .env
docker compose up -d
```

### Из исходников

```bash
git clone https://github.com/andkh1976/PostSync.git
cd PostSync
CGO_ENABLED=1 go build -o postsync .
./postsync
```

> **Важно:** `CGO_ENABLED=1` обязателен — проект использует SQLite через CGo. Установите `gcc` и заголовки C: `build-essential` (Debian/Ubuntu) или `base-devel` (Arch).

---

## Быстрый старт

### 1. Создайте ботов

- **Telegram** — через [@BotFather](https://t.me/BotFather)
- **MAX** — через [dev.max.ru](https://dev.max.ru/docs/chatbots/bots-create)

### 2. Настройте переменные окружения

Скопируйте `.env.example` в `.env` и заполните:

```env
TG_TOKEN=your_telegram_bot_token
MAX_TOKEN=your_max_bot_token

# Для ретроспективной синхронизации (MTProto) — получить на my.telegram.org
TG_APP_ID=12345678
TG_APP_HASH=abcdef1234567890abcdef1234567890
TG_PHONE=+79001234567

# Mini App — URL, по которому доступен веб-интерфейс
MINI_APP_URL=https://your-domain.com:8443/app/

# Хранилище (по умолчанию — SQLite)
# DB_PATH=bridge.db

# Или PostgreSQL
# DATABASE_URL=postgres://user:pass@localhost/postsync
```

### 3. Запустите бота

```bash
./postsync
```

При первом запуске с MTProto бот запросит код подтверждения из Telegram. После успешной авторизации сессия сохраняется в файл `TG_SESSION_FILE` — повторный ввод кода не нужен.

### 4. Свяжите каналы

Настройка выполняется один раз через **Mini App**:

1. Добавьте TG-бота и MAX-бота как администраторов в соответствующие каналы (с правом публикации)
2. Откройте Mini App через кнопку **⚙️ Открыть панель управления** в боте (команда `/start`)
3. В разделе **Связки каналов** нажмите **Создать связку** и укажите ID TG- и MAX-каналов — связка будет создана в один клик

Ручной ввод команд `/crosspost` и пересылка постов больше не требуются.

Дальнейшее управление (направление, синхронизация, живое прослушивание, автозамены) — через Mini App.

---

## Mini App

Mini App — основной интерфейс управления PostSync. Открывается прямо внутри Telegram или MAX без перехода в браузер.

### Как открыть

**Через Telegram:**

1. Напишите боту `/start`
2. Нажмите кнопку **⚙️ Открыть панель управления**

Или настройте постоянную кнопку через BotFather:
- `/mybots` → выберите бота → **Bot Settings → Menu Button → Configure menu button**
- URL: `https://your-domain.com:8443/app/`
- Текст: `⚙️ Настройки`

**Через MAX:**

В настройках MAX-бота укажите `webapp_url`:
```
https://your-domain.com:8443/app/
```

### Разделы

#### 👤 Профиль

- ID пользователя и платформа (Telegram / MAX)
- Имя и username
- Статус подписки

#### 🔗 Активные связки каналов

Список каналов, настроенных через `/crosspost`. Для каждой связки:

- **Направление** — TG → MAX / MAX → TG / оба. Сейчас активен только режим TG → MAX
- **Живое прослушивание** — переключатель активности потока. Выключение останавливает пересылку новых постов без удаления связки

Настройки сохраняются автоматически при изменении.

#### ⏳ Задачи синхронизации

Список запущенных задач ретроспективного скачивания:

- Статус: Ожидание → В процессе → Завершено / Ошибка
- Прогресс-бар
- Канал (TG ID → MAX ID) и период

Раздел автообновляется каждые 5 секунд, пока есть активные задачи.

#### 📥 Ретроспективное скачивание

Скачивает историю TG-канала за указанный период и публикует посты в связанный MAX-канал.

> Требует MTProto (переменные `TG_APP_ID` и `TG_APP_HASH`). Без них раздел недоступен.

1. Выберите связку из списка
2. Укажите период (по умолчанию последние 30 дней)
3. Нажмите **▶ Запустить скачивание**

Задача создаётся в фоне — прогресс отслеживается в разделе «Задачи синхронизации».

**🗑 Сбросить историю** — очищает внутренний маппинг ID сообщений за период. Посты в каналах не удаляются. Используйте, если нужно повторно опубликовать уже пересланные посты.

### Авторизация

Mini App использует стандартный механизм Telegram WebApp (`initData`) для проверки подлинности пользователя. Открытие через кнопку в боте происходит автоматически — никаких дополнительных шагов не требуется.

При открытии напрямую через браузер авторизация недоступна — раздел «Профиль» покажет сообщение об ошибке.

---

## Ретроспективная синхронизация (MTProto)

Для скачивания истории TG-каналов используется Telegram User API (MTProto) — это отдельный клиент, работающий от имени пользователя, а не бота. Стандартный Bot API не предоставляет доступ к истории каналов.

### Настройка

1. Откройте [my.telegram.org](https://my.telegram.org) → **API development tools**
2. Создайте приложение — получите `api_id` и `api_hash`
3. Добавьте в `.env`:

```env
TG_APP_ID=12345678
TG_APP_HASH=abcdef1234567890abcdef1234567890
TG_PHONE=+79001234567
```

4. Перезапустите бота — он запросит код из Telegram для первичной авторизации
5. Сессия сохраняется в `TG_SESSION_FILE` (по умолчанию `tg_session.json`) — повторный вход не нужен

Если `TG_APP_ID` и `TG_APP_HASH` не заданы, sync worker не запускается, а кнопка «Запустить скачивание» в Mini App недоступна.

---

## Команды (legacy)

Текстовые команды сохранены в коде и продолжают работать, однако основной способ управления — Mini App.

### В личке с ботом (кросспостинг каналов)

| Команда | Где | Описание |
|---------|-----|----------|
| `/start`, `/help` | TG или MAX | Инструкция + кнопка открытия Mini App |
| `/crosspost` | TG или MAX личка | Список активных связок |
| `/crosspost <TG_ID>` | MAX личка | Начать настройку новой связки |
| Переслать пост | TG или MAX личка | Показать ID канала или кнопки управления |

### В группах (bridge — устаревший режим)

Функциональность связки групп (`/bridge`, `/unbridge`) сохранена в коде, но в текущей конфигурации сервиса не используется. Актуальный способ — кросспостинг каналов через `/crosspost` и Mini App.

---

## Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TG_TOKEN` | Токен Telegram бота | — **(обязательно)** |
| `MAX_TOKEN` | Токен MAX бота | — **(обязательно)** |
| `TG_APP_ID` | ID приложения Telegram для MTProto. Получить на [my.telegram.org](https://my.telegram.org) | — (нужен для retro sync) |
| `TG_APP_HASH` | Хэш приложения Telegram для MTProto | — (нужен для retro sync) |
| `TG_PHONE` | Номер телефона для первичной авторизации MTProto (формат `+79001234567`) | — |
| `TG_SESSION_FILE` | Путь к файлу сессии MTProto | `tg_session.json` |
| `MINI_APP_URL` | Публичный URL Mini App для кнопки WebApp в боте | — |
| `MINI_APP_DIR` | Путь к папке с файлами фронтенда | `frontend` |
| `DB_PATH` | Путь к SQLite базе | `bridge.db` |
| `DATABASE_URL` | DSN для PostgreSQL. Если задана — SQLite игнорируется | — |
| `WEBHOOK_URL` | Базовый URL для webhook (если не задан — long polling). Пути содержат случайный суффикс для безопасности — выводятся в лог при старте | — |
| `WEBHOOK_PORT` | Порт для webhook-сервера и Mini App | `8443` |
| `LOG_LEVEL` | Уровень логирования: `debug`, `info`, `warn`, `error` | `info` |
| `TG_API_URL` | URL локального [Telegram Bot API сервера](https://github.com/tdlib/telegram-bot-api). Снимает лимит 20 МБ на файлы | — |
| `TG_BOT_URL` | Ссылка на TG-бота (показывается в `/help`) | — |
| `MAX_BOT_URL` | Ссылка на MAX-бота (показывается в `/help`) | — |
| `ALLOWED_USERS` | Белый список Telegram user ID через запятую. Если не задан — доступ открыт для всех | — |
| `TG_MAX_FILE_SIZE_MB` | Максимальный размер файла TG→MAX в МБ (0 = без ограничений) | — |
| `MAX_MAX_FILE_SIZE_MB` | Максимальный размер файла MAX→TG в МБ (0 = без ограничений) | — |
| `MAX_ALLOWED_EXTENSIONS` | Допустимые расширения файлов через запятую. Если не задано — без ограничений | — |
| `MESSAGE_FORMAT` | Формат подписи: `inline` (Имя: текст) или `newline` (Имя:\nтекст) | `inline` |

---

## Статус функций

| Функция | Статус |
|---------|--------|
| Кросспостинг TG → MAX | ✅ Активно |
| Ретроспективная синхронизация (MTProto) | ✅ Активно (требует TG_APP_ID/HASH) |
| Mini App (управление) | ✅ Активно (требует MINI_APP_URL) |
| Автозамены при пересылке | ✅ Активно |
| Retry-очередь | ✅ Активно |
| Кросспостинг MAX → TG | ⏸ Временно отключён (закомментирован) |
| Связка групп (/bridge) | ⚠️ Legacy (работает, не используется в текущей конфигурации) |

---

## Лицензия

[CC BY-NC 4.0](LICENSE) — свободное использование и модификация, коммерческое — только с письменного разрешения автора.

## Разработчик

[bearlogin.dev](https://bearlogin.dev) — разработка ботов, сайтов, лендингов и digital-продуктов под ключ.
