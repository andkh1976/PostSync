Завершаем работу. Предыдущие три спринта (база, движок синхронизации и Mini App) реализованы. Теперь необходимо провести финальную интеграцию, настроить логику ограничений и подготовить проект к запуску.

КРИТИЧЕСКОЕ ПРАВИЛО: Никогда не удаляй существующий код. Если какой-то блок кода (старые обработчики, функции или переменные) заменяется новым — просто закомментируй его с пометкой // DEPRECATED (Sprint 4).

Задачи Спринта №4:

Активация «Спящей» логики (Биллинг и Лимиты):

В файле bridge.go (или там, где реализована основная логика пересылки) внедри функцию-прослойку checkAccess(userID int64) bool.

Эта функция должна проверять статус пользователя в таблице users (поле subscription_end).

Важно: На текущем этапе функция должна всегда возвращать true (полный доступ), но внутри закомментируй реальную логику проверки даты, чтобы я мог включить её одной строчкой позже.

Добавь в настройки лимит на количество сообщений в одной задаче ретро-синхронизации для бесплатных пользователей (закомментируй этот лимит, оставив его как заготовку).

UX/UI полировка Mini App:

В папке frontend доработай интерфейс: добавь индикатор прогресса (Progress Bar) для активных задач из таблицы sync_tasks.

Добавь раздел "Профиль" в Mini App, где будет отображаться ID пользователя и статус его "подписки" (используй данные из API, подготовленного в 3-м спринте).

Убедись, что интерфейс корректно отображается как в темной, так и в светлой теме Telegram и MAX.

Стабилизация и Тестирование:

Проверь механизм предотвращения дубликатов при одновременной работе "Живого потока" и "Ретро-синхронизации" (проверка по таблице messages).

Оптимизируй логирование: убедись, что все системные ошибки MTProto или API мессенджеров записываются в файл лога с понятными описаниями.

Обновление документации (README.md):

Обнови README.md. Опиши новый способ установки через переменные TG_APP_ID и TG_APP_HASH.

Добавь инструкцию по запуску Mini App и краткое описание разделов (Синхронизация, Живой поток, Очистка истории).

Финальная ревизия кода:

Просмотри файлы telegram.go и max.go. Если там остались активные участки кода, которые дублируют функции Mini App, закомментируй их.

После внесения изменений убедись, что проект успешно собирается, а все функции из прошлых спринтов (включая закомментированные) остались в исходных файлах.