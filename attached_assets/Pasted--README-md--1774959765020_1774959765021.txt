Проанализируй текущий код проекта и файл README.md. Нам нужно устранить последние несоответствия между документацией и реализацией, закрепив переход на управление через Mini App.

КРИТИЧЕСКОЕ ПРАВИЛО: Никогда не удаляй существующий рабочий код. Если блок кода заменяется или выводится из эксплуатации, просто закомментируй его с пометкой // DEPRECATED (Sprint 4 Correction).

Задачи по коррекции:

Отключение Legacy-команд (Bridge & Crosspost):

В файле bridge.go (функция registerCommands) закомментируй регистрацию команд /bridge, /unbridge и /crosspost. В README.md они уже заявлены как неиспользуемые.

В файлах telegram.go и max.go закомментируй соответствующие блоки case в циклах обработки сообщений, которые отвечали за логику этих команд и ручной ввод ключей/ID.

Завершение перехода на Mini App (API):

Убедись, что в api.go реализованы или подготовлены заглушки для всех действий, которые раньше делались командами: создание новой связки, удаление связки, настройка автозамен.

Если логика создания новой пары (PairCrosspost) ещё не доступна через API, добавь эндпоинт POST /api/channels/pair, чтобы Mini App мог полностью заменить ручной процесс из раздела «Быстрый старт» в README.

Синхронизация "Быстрого старта" в README:

Обнови раздел "Быстрый старт" в README.md. Удали инструкции по ручному вводу /crosspost <TG_ID> и пересылке постов в личку бота как основного метода связки.

Напиши, что теперь достаточно добавить ботов в каналы и выполнить связку в один клик через интерфейс Mini App.

Проверка "Спящего" биллинга:

Проверь функцию checkAccess в bridge.go. Она должна возвращать true для всех, но содержать закомментированный код вызова b.repo.GetUserProfile(userID) и проверки даты подписки.

Контроль направления MAX → TG:

Еще раз проверь max.go. Функция forwardMaxToTg и все её вызовы должны быть полностью закомментированы, чтобы исключить случайную пересылку в Telegram.

После внесения правок убедись, что проект успешно компилируется, а все закомментированные участки кода сохранены в исходных файлах для истории.